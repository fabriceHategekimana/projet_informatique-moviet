apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: helmcharts_frontend-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/helmcharts_frontend-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/helmcharts_frontend-network: "true"
